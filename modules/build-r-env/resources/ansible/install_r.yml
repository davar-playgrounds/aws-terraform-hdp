---
- hosts: all
  tasks:
    - name: Install epel-release
      yum:
        name: epel-release
        state: present
      become: yes

    - name: Install R
      yum:
        name: R
        state: present
      become: yes

#    - name: Install RStudio
#      yum:
#        name: rstudio-server-rhel-1.1.456-x86_64.rpm
#        state: present
#      become: yes

    - name: Install RStudio
      yum: name=https://download2.rstudio.org/rstudio-server-rhel-1.1.453-x86_64.rpm
      become: yes
